{
    "version": "0.1.0",
    "command": "dotnet",
    "isShellCommand": true,
    "args": [],
    "tasks": [
        {
            "taskName": "build",
            "args": [
                ""
            ],
            "isBuildCommand": true,
            "problemMatcher": "$msCompile"
        }
    ],
    "options": {
        "_comment" : "dotnet core will look perform build on the /src directory, we need to override that here due to our project layout",
        "cwd": "${workspaceRoot}/src/app"
    }
}